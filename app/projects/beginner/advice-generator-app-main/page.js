export default function Page() {
    //Get the essential markup.
    //Add motion.

    //Trigger the function to call the api.
    //Get a spinner loading.
    //Move the desktop preview to the pages
    //Add the project to the overall list.
    return <div>Advice Generator App Main</div>
}
